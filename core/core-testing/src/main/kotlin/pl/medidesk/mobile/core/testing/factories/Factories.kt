package pl.medidesk.mobile.core.testing.factories

/**
 * Placeholder for test-data factory builders (WO-MOB-024, Faza 0).
 *
 * Faza 3: add companion-object / top-level builders with sensible defaults and named overrides,
 * e.g. `makeParticipant(id = ..., checkedIn = false)`, `makeEvent(...)`, building core-model types.
 * Keeping fixtures here (vs duplicated per test) prevents drift when model shapes change.
 */
internal object Factories
