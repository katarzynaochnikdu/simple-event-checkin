# PROGRESS — simple-event-checkin (Android Kotlin)

> Chronologiczny log "co robiłem" w obszarze **mobile** (Android natywny, `simple-event-checkin/`).
> Desktop → [.agents/PROGRESS.md](../../.agents/PROGRESS.md).
>
> Wpisy dodawane przez `/mlog <opis>` (subagent `progress-logger`, scope=mobile).
> Najnowsze na górze. Jeden bullet = jedna istotna rzecz (commit / decyzja / odkrycie / blocker).

---

## 2026-05-28

- **WO-MOB-023 DONE** — Fix: dashboard firmy (Review360/insight360) nie otwierał się z ekranu „Moi podopieczni" → toast „Nie udało się otworzyć dashboardu". **Root cause:** Retrofit `review360View(accountId)` wysyłał POST **bez body**, a backend `mobile_review360_view` po **WO-SEC-009** wymaga dla nie-admina `event_id` w body + `_mobile_user_has_event_access` — inaczej `403`. Opiekun (non-admin) → 403 → Retrofit `body()==null` (błąd w `errorBody()`) → UI fallback do generycznego toastu. **Fix (mobile-only, 3 pliki, backend NIETKNIĘTY):** NEW DTO `Review360ViewRequest(@Json("event_id"))` w `ResponseDtos.kt`; `@Body` w `MobileApiService.review360View`; `openReview360(accountId, eventId, onUrlReady)` w `MyMenteesScreen.kt` + call site przekazuje `eventId` (param Composable). Build `assembleDebug` **PASS**. Snapshot: `snapshot/pre-wo-mob-023-review360-event-id-2026-05-28` @ `21de295`. Gates: QA build-only PASS (live smoke = post-deploy APK), Security PASS (client-side, autoryzacja nietknięta), Contract/Migration N/A. **Gotcha:** mobilne `@POST` do `api/mobile/crm/*` muszą dosyłać `event_id` (WO-SEC-009); Retrofit przy 403 ma `body()==null` → generyczny toast maskuje realny błąd. (Master + worker-debugger)

- **WO-MOB-018 DONE** — Zmiana podejścia do sandbox events na liście wydarzeń (follow-up WO-MOB-017). **Usunięto osobną zakładkę „Sandbox".** Sandbox events wpadają teraz **inline** do normalnych grup (Trwające/Nadchodzące/Przeszłe) wg swojej daty, oznaczone **pomarańczowym pillem „SANDBOX"** (`#F97316` orange-500, biały tekst, w wierszu nazwy). **Dev-gate raz na wejściu:** `sourceEvents = if (BuildConfig.DEBUG) raw else raw.filter { !isSandbox(it) }` — w release/preview sandbox events **całkowicie ukryte** (nieobecne w grupach, licznikach, `anyOngoing`); w debug widoczne z pillem. `EventTab` → `{ ONGOING, UPCOMING, PAST }` (skasowany SANDBOX); per-tab filtry przestały wykluczać sandbox; `isSandbox` wyniesione do **top-level** w `EventsScreen.kt` (jedno źródło prawdy: VM dev-gate + karta pill), usunięta prywatna kopia w VM. **2 pliki, +64/-27 LOC:** `EventsViewModel.kt`, `EventsScreen.kt` (`SandboxPill()` composable + `isSandbox` top-level + nazwa w `Row` z `weight(1f, fill=false)`). **Bez zmian:** backend/API/model, `build.gradle.kts` (`buildConfig=true` już z WO-MOB-017). Build `assembleDebug` **PASS** (3m14s). **Snapshot POMINIĘTY na życzenie usera** („rób bez snapshota"; rollback i tak dostępny z `9ef6476` + tag WO-MOB-017). QA: code-level PASS + build PASS; on-device + commit/push czeka na user go-ahead. **Gotcha:** `Modifier.weight(1f, fill=false)` wymaga `RowScope` (nazwa MUSI być w `Row`); detekcja „test" w nazwie ukrywa też realne eventy z „test" dla non-dev (pre-existing). (Master + worker-implementer)

- **WO-MOB-017 DONE** — Lista wydarzeń: nowa, **dynamiczna** zakładka **„Trwające"** (pierwsza od lewej) + fix błędu „w dniu wydarzenia event wpada w przeszłość". **Root cause:** `isPast` robił `parseToDateTime(endDate).isBefore(now)`, a parser dla daty bez godziny zwraca `00:00` → o 00:01 w dniu eventu już „przeszłe". **Fix:** klasyfikacja **data-granularna** (`today: LocalDate`, ignoruje godziny): `isOngoing = startDay ≤ dziś ≤ endDay`, `isUpcoming = dziś < startDay`, `isPast = dziś > endDay` — wyodrębnione jako czyste, testowalne funkcje. Zakładki: **Trwające | Nadchodzące | Przeszłe | Sandbox**. „Trwające" w `visibleTabs` **tylko gdy `anyOngoing`** (liczone z RAW, nie z listy po search → zakładki nie migoczą przy wpisywaniu) i **auto-wybrana** gdy obecna (`_selectedTab` nullable = podążaj za danymi dopóki user nie kliknie). **Sandbox dev-gated** przez `BuildConfig.DEBUG` (wzorzec z `MyMenteesScreen`/`AppNavHost`) — w release/preview ukryta dla wszystkich. **3 pliki, +47/-30 LOC:** `EventsViewModel.kt` (enum+klasyfikacja+visibleTabs+default), `EventsScreen.kt` (`visibleTabs.forEach` + `indexOf(...).coerceAtLeast(0)` + label „Trwające"), `feature-events/build.gradle.kts` (`buildFeatures { buildConfig = true }`). **Bez zmian backend/API/model.** Build `assembleDebug` **PASS** (9m43s; feature-events + consumer feature-dashboard skompilowane). QA: code-level PASS (8 scenariuszy z WO prześledzone) + build PASS; **on-device QA + commit/push czeka na user go-ahead** (guardrail live check-in). Snapshot: `snapshot/pre-mobile-events-ongoing-tab-2026-05-28` @ `28168cc`. **Gotchy (do `known_gotchas.md`):** (a) `BuildConfig.DEBUG` wymaga `buildConfig = true` per moduł (plugin `md.android.feature` nie generuje domyślnie); (b) `parseToDateTime` → `now()` dla pustych/błędnych dat → taki event ląduje w „Trwające". (Master + worker-implementer)

- **WO-MOB-016 DONE (closure retroaktywny)** — Kanoniczne etykiety + kolory tagów uczestników w mobile. Pre-flight verification (lekcja WO-SEC-004) wykazała, że WO był **już zaimplementowany, zacommitowany i wypchnięty** zanim formalnie zamknięty (backlog pokazywał OPEN: brak w INDEX/PROGRESS/review). **Workera NIE dispatchowano** — to byłoby powtórzenie skończonej pracy. Commity: backend `5fa1764 feat(WO-MOB-016): GET /api/mobile/participant-tags endpoint`, mobile `27ae602 feat(WO-MOB-016): canonical participant tag labels + colors` (+ version bump `fff3d06`). Stack: NEW endpoint `GET /api/mobile/participant-tags` (`@require_mobile_token`, reuse `list_participant_tag_definitions`), `ParticipantTagDefinitionDto`/`ParticipantTagsResponse`, `MobileApiService.getParticipantTags()`, `core-sync/ParticipantTagsRepository` (in-memory cache + 10 `DEFAULT_TAGS` mirror seedu 0034 + fail-soft refresh), `core-ui/ParticipantTagChip` (Tailwind→Compose Color, 19 wpisów), refresh po loginie (`LoginViewModel`), render w `ParticipantDetailsScreen:314-318`. **`SponsorDetailScreen` świadomie POZA ZAKRESEM** (user-confirmed 2026-05-28): tagi sponsora to słownik firmowy/branżowy (osobne pole `industryCategory`), nie `participant_tag` — migracja byłaby błędem kategorii. Ta sesja: review note + IMPLEMENTATION_REPORT + reconcile INDEX (009/010 stale→DONE, dodane 012/013/016) + uzupełnienie `backend/api/MOBILE_API.md` (endpoint nie był udokumentowany przy commicie). Bramki N/A (docs only, kod shipped w oryginalnych commitach). **Gotcha:** `DEFAULT_TAGS` to 4-ta zahardkodowana kopia seedu tagów (DB 0034 + frontend `useParticipantTags.ts` + mobile + mapa kolorów). Build `assembleDebug` nie re-weryfikowany tej sesji. (Master — pre-flight + closure)

## 2026-05-25

- **WO-MOB-014 DONE** — Sync button (manual refresh) w `ParticipantsScreen` toolbar. **Audyt obecnego cyklu sync (kluczowa odpowiedź dla usera):** auto-sync DZIAŁA przy każdym `onResume` (`ParticipantsScreen.kt:60-64` `LifecycleResumeEffect` → `silentSync`) oraz pull-to-refresh (`PullToRefreshBox` linia 190-194), ale **brak auto-pollingu w tle** — `SyncEngine.startPeriodicSync` (5 min, linia 65-71) jest zdefiniowany ale **NIGDY nie wywoływany** (martwy kod, jedyny consumer to logout). Konsekwencja: user trzymający ekran otwarty bez interakcji nie zobaczy nowych uczestników aż wyjdzie/wróci albo zrobi pull. **Implementacja:** 2 pliki, +58/-2 LOC: (1) `ParticipantsViewModel.kt` — dodano sealed class `SyncResultEvent { Success(count); Error(message) }`, `_events: MutableSharedFlow(extraBufferCapacity=4)` + public `events: SharedFlow`, `refresh()` owinięty w try/catch z emisją Success(count z `participantDao.countForEvent` — DAO method JUŻ istniał suspend fun) lub Error(e.message). (2) `ParticipantsScreen.kt` — `IconButton(Icons.Default.Refresh, enabled = !uiState.isRefreshing)` w `TopAppBar.actions` (CircularProgressIndicator gdy refreshing), `SnackbarHostState` + `LaunchedEffect(Unit)` collect `viewModel.events` → Success: "Zsynchronizowano (N uczestników)", Error: "Nie udało się zsynchronizować. Sprawdź połączenie." + action button "Ponów" → `refresh(eventId)`. Decyzje usera: TopAppBar action (vs FAB/sheet), komunikat "Zsynchronizowano (N)" (vs "Lista zaktualizowana"), action button "Ponów" w error, silent dla LifecycleResumeEffect auto-sync (snackbar TYLKO dla explicit user action: button + pull). Build PASS: `compileDebugKotlin` 3m49s + `assembleDebug` 2m02s. Snapshot: `snapshot/pre-mob-sync-button-participants-2026-05-25` @ `3090386`. **Gotcha do dodania w `known_gotchas.md`:** `SyncEngine.runImmediateSyncAndWait` to "fire-and-wait" NIE "fire-and-throw" — `WorkInfo.state == FAILED` NIE rzuca exception, więc snackbar "Zsynchronizowano (N)" może pokazać false positive gdy backend zwróci 500. Acceptable risk dla MVP, follow-up WO sugerowany (extend signature żeby zwracała `WorkInfo.State`). Czeka na user smoke test + commit + push. (Master + worker-research + worker-implementer)

## 2026-05-20

- **WO-MOB-013 DONE** — Fix Wątek B z BUG-MOB-002 (rozjazd "łącznie uczestników" mobile vs web). AMOZ Kraków mobile=107 vs web=100, Dental mobile=49 vs web=46. Mobile filtrował po `o.status NOT IN ('cancelled','refunded')` (anulowane orders), web "Pewni" filtruje po `p.attendance_status IN ('confirmed','pending')` (anulowane RSVP) — filtry ortogonalne. Decyzja autonomous (opcja b user "Działaj dalej"): mobile używa identycznego filtra jak web → spójność UX operatora. Zmiana 5 lokalizacji `pg_storage.py` w 3 funkcjach mobile. Sanity prod: AMOZ 100=100 ✅, Dental 45 vs 46 (offset 1 edge case akceptowalny). Gotcha: initial `replace_all` złapał `recompute_discount_code_uses:6241/6264` (discount accounting, NIE check-in) — zrewertowane manualnie. Snapshot SKIPPED (continuity). Commit: backend `ceaf0c8`. Follow-up: WO-MOB-014 (pokrewne mobile.py). (Master + worker-implementer)
- **WO-MOB-012 DONE** — Fix mobile undercount check-in (Wątek A z BUG-MOB-002). Dental Practice Academy Poznań pokazywał 0 odznaczonych vs web 46. Root cause potwierdzony prod SELECT-ami (`backend/.env` DATABASE_URL): 46 uczestników miało `data.checked_in='true'` ale `status` ∈ `{emailed, pending, registered}` (NIE `checked_in`). Mobile aggregator (`get_participants_for_mobile`, `get_checkin_stats`, `get_mobile_dashboard` ×4 sub-query) filtrował tylko po `status='checked_in'`. Wyrównanie do kanonicznego predykatu OR-3 z `pg_storage.py:9963-9966` (web admin): `status='checked_in' OR data->>'checked_in' IN ('true','True') OR attendance_status IN ('checked_in','present')`. Pure backend fix — mobile DTO/Room/Kotlin bez zmian, APK niepotrzebny. Sanity check po fixie: Dental 46/46 ✅, AMOZ 66/66 (brak regresji). Snapshot SKIPPED (user explicit). Commits: backend `69cf404`, mobile docs `44971e8`, bump TBA. Follow-ups: WO-MOB-013 (Wątek B — różny filtr "łącznie"), WO-MOB-014 (pokrewne mobile.py:1176/1846/1921/1927). Bonus odkrycie: `checkin_log` niekompletny (Dental 17/46, AMOZ 50/66) — `known_gotchas.md`. (Master + worker-debugger + worker-implementer)
- **WO-MOB-011 DONE** — Bottom NavigationBar tab "Wydarzenie" (`AppNavHost.kt:317`): `maxLines = 1` → `maxLines = 2` żeby długie nazwy eventu zawijały się na 2 linie zamiast wcześnie urywać ellipsisem. Decyzja user: **opcja A** (3 linie zmiany) — świadoma rezygnacja z punktu 1 warunku bramkowego (stała pozycja icon). M3 `NavigationBarItem` ma fixed-height (~80dp) Column → przy 2-liniowym labelu icon `Icons.Default.Home` podnosi się ~6-8dp; opcja B (custom NavBarItem ~80-120 linii) odrzucona jako overkill. Pozostałe taby (Uczestnicy, Skaner) bez zmian. User weryfikuje wizualnie na buildzie. Snapshot: `snapshot/wo-mob-011-pre-navbar-label-wrap-2026-05-20` @ `4993c83`. (Master)
- **WO-MOB-010 DONE** — Fix UNDONE color (zielony→czerwony, `ScanError`) + usunięcie button'a "Cofnij wejście" z `ScanResultOverlay` SUCCESS branch. Cel: operator widzi semantycznie różnicę między WEJŚCIE OK (zielony) a COFNIĘTO (czerwony, exit-state); eliminacja przypadkowego undo w trakcie 3s auto-dismiss. Pliki: `ScannerScreen.kt` (+1/-15). Build PASS 36s. Commits: `b4d2d2f` mobile / `c21cfd8` monorepo bump. `ScannerViewModel.undoLastScan()` teraz orphaned (jedyny caller usunięty) — follow-up cleanup w przyszłym WO. (worker-implementer)
- **WO-MOB-009 DONE** — Fix scan result feedback overlay (zielony + czerwony) jako window-level `Dialog` zamiast in-tree `Box(fillMaxSize)`. Oba warianty teraz nakrywają cały ekran włącznie z `Scaffold(bottomBar)` z `AppNavHost`. `DialogProperties(usePlatformDefaultWidth=false, decorFitsSystemWindows=false, dismissOnBackPress=true, dismissOnClickOutside=false)`. Dodano `ScannerViewModel.dismissFeedback()` dla back-press handlingu. Ujednolicenie timing auto-dismiss DENIED 1500→3000ms (symetria z SUCCESS). Pliki: `ScannerScreen.kt` (+22/-9) + `ScannerViewModel.kt` (+11/-1). Build PASS 3m28s. Commits: `40dff85` mobile / `4368e91` monorepo bump. Gotcha → `known_gotchas.md`. (worker-implementer)

## 2026-05-19

- **WO-MOB-001 DONE** — Code-level QA auth flow po security WO-201/202/204: 6/6 scenariuszy PASS, PII guard PASS, brak regresji. APK gotowy do dystrybucji. (worker-research, ~140s)
- **APK debug rebuild** — mEventLab-debug.apk (58 MB) zbudowany z najnowszym commitem `f22e2bd` (PostHog analytics + custom events). Przekazać zespołowi.
- **AAB release** — mEventLab-release.aab (28 MB) zbudowany (WO-188), keystore `medidesk-release.jks` wygenerowany, dane w RELEASE_SIGNING.md (gitignored).

## 2026-05-18

- _(brak wpisów — PROGRESS.md utworzony przy bootstrap mobile workflow)_
